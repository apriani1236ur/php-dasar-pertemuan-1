<?php

$a = true;
$b = false;

// Operator Logika
echo var_dump($a && $b) . "<br>"; // AND (&&)
echo var_dump($a || $b) . "<br>"; // OR (||)
echo var_dump(!$a) . "<br>";      // NOT (!)
echo var_dump($a xor $b) . "<br>"; // XOR (xor)

?>